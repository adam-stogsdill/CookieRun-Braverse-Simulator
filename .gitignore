__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
.DS_Store

# Downloaded card art (190 MB) — `python fetch_images.py` fetches it.
card_images/

# Generated Tabletop Simulator deck sheets — `python build_tts_sheets.py`.
tts_sheets/

# PyInstaller output — `pyinstaller braverse.spec`.
build/
dist/
.venv-build/

# Decks built in the browser's deck builder tab.
saved_decks.json

# Games the viewer kept, one JSON file each — `viewer/replays.js`.
replays/

# Player profiles: one encrypted file each, plus the key that opens the ones
# with no passphrase. Nothing here belongs in a repository — `braverse/profile.py`.
profiles/

# Training / evolution output.
*.log
*.err

ClAUDE.md

*.pt
rl_agent_*
*.sh
# Standalone builds — `python build_release.py`.
release/

*.exe